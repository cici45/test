using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreatePlaneGame : MonoBehaviour
{  
    void Update()
    {
        //if (Input.GetKey(KeyCode.A)) MessageCenter.SendMsg("PlaneGameStart", true);//²âÊÔ´ıÉ¾³ı
        //if (Input.GetKey(KeyCode.S)) MessageCenter.SendMsg("ContinueOrStopGame", false);//²âÊÔ´ıÉ¾³ı
        //if (Input.GetKey(KeyCode.D)) MessageCenter.SendMsg("ContinueOrStopGame", true);//²âÊÔ´ıÉ¾³ı
        //if (Input.GetKey(KeyCode.F)) MessageCenter.SendMsg("PlaneGameStart", false);//²âÊÔ´ıÉ¾³ı
    }
}
